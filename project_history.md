# UDE Points — Revision History

Chronological record of fixes, decisions, and audits. `canonical_project_state.md` reflects only the current state; this document explains how it got there. Not required reading to continue the project — consult it only when the "why" behind a past decision matters.

---

## 2026-08-21

1. **Age Sign Fix (superseded same day).** `_age_multiplier` no longer wraps the fitted logistic slope in `abs()` uniformly for both sides. First pass applied the unwrapped signed slope to *both* `opponent` and `own` sides — measurably shrank the own-age bonus for wins past reference age into a penalty (own-side post-slope is empirically negative). Corrected same day, see #7.

2. **Bonus Anchor Standardization.** `streak_adjustment` seeded with and measured against `raw_base_points` (±3), matching `title_defense_bonus`, before both feed `combined_bonuses` and get scaled once by `perf_scale`. Removed the prior compounding path where streak bonus rode on top of championship/dominance multipliers on title-bout fights.

3. **Rolling Age/Method Calibration Window.** `_build_temporal_calibration_cache` moved to a trailing 5-year window (`CALIBRATION_ROLLING_WINDOW_YEARS`), falling back to full expanding history only below `CALIBRATION_MIN_FIGHT_OBSERVATIONS` (1,000) fights. Confirmed on the live dataset: fallback engages 1999–2011, rolling window active from 2012 onward (2026 uses 2,577 fights vs. 8,226 under the old expanding approach).

4. **`ABSOLUTE_SWING_CAP` Diagnostics.** Added execution logging + `df.attrs` recording how often the 60-point circuit breaker binds. Measured: 0/16,904 (0%) — cap was dormant, not load-bearing.

5. **PDI Margin Multi-Layer Review (decision, no code change).** Audited whether `dominance_adjustment`, `perf_scale`, and the method×PDI residual constitute redundant point creation from the same signal. Finding: `dominance_adjustment` and `perf_scale` both derive from the identical `pdi_margin / PDI_MARGIN_SCALE`, so they move in the same direction on every fight (~35% of a title-defense fight's total PDI-driven swing traced to this directional stacking). `perf_scale` structurally cannot generate points on its own (shrink-only, bounded 0.20–1.0) — accepted as a bounded tradeoff, not a defect.
   - Considered decoupling `perf_scale` onto method decisiveness or `phases_won` — rejected. Method already has a dedicated calibrated channel (`method_pdi_residual_points`); `phases_won` is itself a discretization of the same PDI data `dominance_adjustment` already consumes. No unclaimed orthogonal signal existed for `perf_scale` to borrow without creating a new collision.

6. **`min_fights` Floor on Shrunk-Rate Ranking.** `rank_fighters_by_shrunk_ude_rate` gained an optional `min_fights` parameter. Bayesian shrinkage alone (`prior_strength=10`) was insufficient at very small n — a 4-fight career (Murilo Bustamante, 2 of 4 fights title-bout wins) still ranked top-20 despite an 84% shrinkage haircut, with rank unstable (8→55) across `prior_strength` 5–30. `population_mean_rate` is still computed over the full unfiltered population so the shrinkage target itself isn't biased by the floor.

7. **Own-Age Achievement vs. Opposition-Quality Split.** `_age_multiplier` given a different sign convention per side. `opponent` side keeps the signed `opponent_post_slope` (an opposition-quality discount, correctly signed). `own` side switched to `abs(own_post_slope)` deliberately — an achievement-under-adversity signal, matching how every other UDE component (`higher_rated_opponent_bonus`, `opponent_quality_adjustment`) rewards clearing a harder bar with *more* credit, never less. Not a reversion to #1's bug: that bug applied `abs()` without asking whether both sides measure the same kind of quantity — they don't.

8. **Smoothed Own-Age Opponent Gate.** `own_age_adjustment`'s hard cutoff (`opponent_age > reference_age` → bonus fully zeroed) replaced with `_own_age_gate_scale`, a sigmoid centered on `reference_age`, 3-year width. Eliminated the discontinuous jump in a fighter's own score from a marginal (sub-year) change in opponent age.

9. **Bonus Component Columns Persisted.** `title_defense_bonus`, `streak_bonus`, `age_adjustment`, `own_age_adjustment`, `rematch_adjustment` (per `_fighter_1`/`_fighter_2`) now written per-fight — previously computed but discarded. Verified purely additive before the next fix landed: bit-identical `ude_points_post_fight_*` (max abs delta 0.0 across all 8,564 fights).

10. **Rolling-Window/Expanding-Fallback Boundary — measured, not fixed.** At the 2011→2012 transition, the calibration cache switches regimes for the first time (2012 is the first year with ≥1,000 fights in a trailing 5-year window). Measured directly: the 2011→2012 delta in fitted `reference_age`/`own_post_slope`/`opponent_post_slope` is *smaller* than several purely within-regime year-over-year deltas elsewhere (e.g. 2012→2013, 2025→2026) — not a distinguishable artifact. Left as-is.

11. **CRITICAL — Neutral-Calibration NaN Bug in `_age_multiplier`, found and fixed.** Discovered while investigating an unexpected top-20 reshuffle after #7/#8. `_neutral_age_calibration()` (used 1999–2007, insufficient prior history) sets `reference_age = NaN`. The guard `if age <= reference_age: return 1.0` never short-circuits against NaN (all NaN comparisons are False in Python/numpy), so execution fell through into `slope * NaN = NaN` math, and `max(AGE_MIN_MULTIPLIER, min(AGE_MAX_MULTIPLIER, multiplier))` silently resolved that NaN to the concrete 1.5x ceiling instead of a neutral no-op (Python's `min`/`max` keep the first argument compared against NaN). Net effect: every decisive fight scored under neutral age calibration got a flat, unconditional +50% bonus (wins) or 50%-larger penalty (losses) via `age_adjustment` — confirmed on 602/606 fights (99%) across 606 fights (~7% of the dataset) in 1999–2007. `own_age_adjustment` had the same defect independently but was accidentally neutralized as a side effect of #8's explicit `pd.isna(reference_age)` guard — `age_adjustment` alone was still live going into this pass. Fixed by adding the guard directly to `_age_multiplier`.

12. **Two harmless cleanups in `ude_points_algorithm.py`.** `championship_bonus`'s `if result=='W'`/`elif result=='L'` branches did the identical thing (both `points*2.0`) — collapsed to one condition. `dominance_adjustment` accepted a `method_mapped` parameter it never read (the docstring already says "method-neutral") — removed, and its one call site updated. Verified bit-identical output before/after.

13. **Architectural review of `ude_points_feature_engineering_pipeline.py`.** First read-through of this file this session. Findings: a hard threshold cliff in `calculate_phase_magnitude_and_pdi`'s takedown/submission magnitude (fixed, see #15); `map_weight_class`'s substring-based classification was order-dependent and fragile — `"Heavyweight"` is a literal substring of `"Light Heavyweight"`, and correctness relied entirely on dict insertion order (fixed, see #14); `who_won_*`/`dominant_fighter`/`phases_won` and `rematch_column`/`is_rematch` were initially flagged as dead code because `ude_points_algorithm.py` doesn't consume them — **incorrect call, retracted.** These are intentionally retained for narrative/visualization use outside the scoring path (phase-win breakdowns, rematch-depth display); not everything in this pipeline exists to feed UDE scoring.

14. **`map_weight_class` longest-match fix.** Sorted the weight-class dict by name length (descending) before the substring-containment loop, instead of relying on insertion order. Verified against all 100 distinct raw `weight_class` strings in the live dataset: zero output changes — purely defensive, no rescoring needed.

15. **PDI takedown/submission magnitude — smoothed the hard cliff.** `calculate_phase_magnitude_and_pdi`'s `td_mag`/`sub_mag` picked between a "close" formula (capped 0.35) and a "decisive" formula (0.36+) via a hard `count > N and proportion >= P` gate — a real value-gap and a disproportionate jump right at the boundary, on the single most influential input in the whole scoring system (`pdi_margin` drives `dominance_adjustment`, `perf_scale`, and the method residual simultaneously). Replaced with `_dominance_magnitude`: a soft-AND blend (product of two sigmoids, over landed-count and proportion) between the same two curves.
    - First-draft widths (`count_width=1.0`) were checked numerically before shipping and found to reintroduce exactly the problem the original gate existed to prevent: landing 2 of 2 attempted takedowns (a tiny sample) jumped from 0.35 to 0.466 — a 33% inflation for a 2-takedown sample. Root cause: takedown/submission counts are integers, so there's nothing to smooth *between* consecutive counts — a loose width just lets the "decisive" curve bleed into small samples instead of interpolating.
    - Tightened to `count_width=0.10`: 1/1, 2/2, and 3/3 (all landed-vs-opponent's-0) now land within 0.004 of their old capped value — the original small-sample protection is preserved almost exactly. The actual value this fix adds is in the proportion axis (genuinely continuous — no more instant flip at exactly 80%/75%) and in closing the 0.35→0.36 value-gap itself, not in the count axis, which integers make impossible to meaningfully smooth.
    - Recomputed PDI-dependent columns on `v2_6.csv` directly (all raw inputs — strikes, control time, takedowns, submissions, knockdowns — already present; the separately-referenced `all_fights_data_processed_engineered_and_ready_for_ude_points` raw file wasn't needed and isn't present in the project directory). 2,966/8,564 fights (35%) had a `pdi_margin` change, mean magnitude 0.032, max 0.37 — modest, as expected from a fix touching only two of five phase magnitudes. Full UDE re-run: 0 NaN, 0% swing-cap bind, ranking membership and order unchanged (same 20 fighters, same order — values shifted at the 2nd-3rd decimal), stability re-confirmed (18–19/20 overlap across `prior_strength` 5–20).

16. **Second architectural pass on `ude_points_feature_engineering_pipeline.py`, assume-nothing re-derivation.** Re-read the file fresh rather than trusting the first pass's verdict. Two new findings, both verified against real data before acting:
    - **Latent NaN-poisoning risk** in `update_career_means`, `add_dynamic_strike_accuracy`/`_defence`, `add_dynamic_td_accuracy`/`_defence`: each accumulates a per-fighter running total via unchecked `+=`, so one NaN in a raw landed/attempted stat would silently poison that fighter's total — and every derived column built from it — to NaN for the rest of their career. Checked: zero NaN currently present across every column these functions touch, so not live. Fixed anyway with a `fillna(0)` guard at the top of `engineer_all_features`, matching `_to_num`'s existing NaN-as-0 convention elsewhere in the same file — same shape of risk as item #11, deliberately closed before it could ever trigger rather than after.
    - **`standing_sig_strikes_landed_per_min_*` silently never generated.** `add_time_and_per_min_features` (step 9) ran before `add_standing_sig_strikes_columns` (step 10) — the per-minute step scans `df.columns` for every `*_landed`/`*_attempted` column present *at that point*, so the source column for this one variant didn't exist yet when the scan ran. Every other landed-stat got its per-min column; only this one silently didn't. Fixed by swapping the step order (standing-sig-strikes now runs first) — zero dependency conflict either direction.
    - Re-confirmed the PDI-cliff fix, `map_weight_class` fix, and the `who_won_*`/`rematch_column` "not dead code" correction all still hold on fresh reading.

17. **Full clean rebuild, prompted by "make sure old columns aren't sitting beside new ones."** Every fix above through this point had been applied incrementally, patching `v2_6.csv` in place — never a from-scratch rebuild. Attempting one immediately surfaced the exact risk being asked about: `engineer_all_features`'s functions add columns via `pd.concat`, which creates a genuine *duplicate*-named column (not an overwrite) when the input already contains that name — re-running the pipeline against the already-processed file crashed inside `quality_score` with "truth value of a Series is ambiguous," because `is_champion_fighter_1` (and everything else) now existed twice and indexing it returned a Series instead of a scalar.
    - Determined the true raw/derived column split **empirically**, not by manual enumeration (too easy to miss one): ran each pipeline stage in sequence starting from a candidate raw set, checked `df.columns.duplicated()` after every single stage, and fed any collision back into the drop-list for the next attempt. Converged after 19 iterations on a 75-column true-raw base. Manually enumerated the 30 `ude_points_algorithm`-output columns separately (since those are written via safe in-place `.at` assignment, never `pd.concat`, so they don't collide the same way) and verified the guess was exactly complete (0 missing, 0 extra) by diffing before/after `calculate_ude_points_with_ablation`'s own column set.
    - Why the algorithm-output columns needed dropping too, even though `.at` assignment can't duplicate: no-score rows (NC/DQ/Overturned) `continue` before writing most diagnostic columns (`method_pdi_residual`, `title_defense_bonus`, `age_adjustment`, etc.) — so if those columns already held values from a prior run, those specific rows would silently keep stale data forever while every scored row got fresh values. Dropping everything derived first, for every row, was the only way to guarantee no stale leftovers anywhere, not just no duplicate names.
    - Ran the full pipeline (`engineer_all_features` → `calculate_ude_points_with_ablation` → `add_ude_points_difference_columns`) on the clean 75-column base: 8,564 × 256 columns, zero duplicates at every stage. Diffed against the incrementally-patched file: exactly 6 new columns (`standing_sig_strikes_*_per_min_*`, the step-order fix from #16 finally taking effect — the earlier PDI-only patch never touched that part of the pipeline, so it had no way to surface this), everything else within 0.0001 (floating-point noise, not a real difference). Re-verified: 0 NaN, 0% swing-cap bind, ranking identical (same 20 fighters, same order, same values to reported precision), stability reconfirmed (18–19/20 overlap across `prior_strength` 5–20). Promoted as the current `v2_6.csv`.
    - Takeaway for next time: incremental patching is fine for verifying one fix in isolation, but only a full raw-to-final rebuild actually proves the shipped file matches the current code everywhere, not just wherever was last touched.

18. **`dataset_processing_pipeline.py` added to the project; null-event_date rows caught at the source.** User supplied a genuinely raw (pre-feature-engineering) dataset, `fights_up_to_islam_garry_ready_for_features.csv` (8,566 fights, 75 columns), plus the ETL file that produces it. Flagged that 2 of the 8,566 rows had null `event_date` and had been manually dropped before use, and asked where the automated version of that drop should live.
    - `event_date` is created in `run_etl_pipeline` via a left join against `events_df`; a null here means the join found no matching event (here: "Road to UFC 4.5 + 4.6," likely too new for the events source at scrape time). The existing `validate_transformed_data` only tracks an aggregate null *percentage* against a 5% threshold — 2/8,566 (0.02%) would never trip it, so this category of problem was invisible to the file's own validation.
    - Added `drop_rows_with_null_event_date(df)` to `dataset_processing_pipeline.py`, called right after column standardization in `run_etl_pipeline` (the first point `event_date` has its final name) — reports exactly which rows it drops (event name/URL, fight URL, bout, fighter names) rather than discarding silently, since a failed join can also mean the events source itself is missing data worth someone's attention. Also called defensively at the top of `engineer_all_features` (imported from the ETL file), since that function shouldn't have to assume its input necessarily came through the fixed ETL step.
    - Ran the full pipeline (`engineer_all_features` → `calculate_ude_points_with_ablation` → `add_ude_points_difference_columns`) on the raw file: the 2 null rows were caught and reported, 8,564 fights scored, 256 columns, zero duplicates, 0 NaN, 0% swing-cap bind. Compared against the current `v2_6.csv` by `fight_url`: **identical fight set, zero difference in any UDE point across all 8,564 fights.** Strongest validation yet that the pipeline is genuinely reproducible from independent raw data, not merely self-consistent under the reverse-engineered rebuild in #17. `v2_6.csv` didn't need to change.

19. **Gap in #18: the check only covered freshly-scraped rows, not `current_dataset`.** User found 2 null-`event_date` rows already sitting in their `current_dataset` (the historical file being appended to on each ETL run) — unclear how they originally got there, predating this fix. The step-3b check only runs on the newly-scraped `df`, *before* `current_dataset` is merged in at step 13, so those rows would pass through `run_etl_pipeline` unchecked and land in its output.
    - User's own read was that this was probably fine to ignore, since `engineer_all_features`'s defensive safety-net call would catch them on the next stage regardless. Correct as far as it goes, but only holds if every consumer of the ETL output goes straight into feature engineering next — anyone taking `run_etl_pipeline`'s output directly (saved to CSV, inspected, used elsewhere) would still get silently-unclean data, defeating half the point of adding the check to the ETL layer at all.
    - Fixed rather than left as a known gap, given how cheap it was: added a second `drop_rows_with_null_event_date` call right after the `current_dataset` concat (step 13), after the `pd.to_datetime(..., errors='coerce')` line so it also catches any date string that failed to parse, not just originally-null ones. Verified directly with a synthetic `current_dataset` containing a stale null-date row — caught, reported, dropped. Re-ran the full raw-file pipeline afterward to confirm no regression: still 8,564 fights, 0 duplicates, and identical to the locked `v2_6.csv` to floating-point noise (5.68e-14, same level as before this change).

### Locked GOAT ranking — how the list moved

Three versions were produced same-day as the fixes above landed:
- **v1** (post #1–4, pre #7–11): included Tim Sylvia, Matt Hughes, Chuck Liddell, Randy Couture in the top 20.
- **v2** (post #7–9, pre #11): those four dropped out. At the time this was attributed to the own-age achievement fix not helping loss-heavy careers — **that explanation was incomplete.** The dominant cause was actually #11's bug (still live in v2): all four fighters' early-2000s careers fall inside the 1999–2007 neutral-calibration window and were absorbing the erroneous flat +50%/-50% `age_adjustment` swing on nearly every early fight, inflating v1's numbers for them specifically. v2 accidentally corrected `own_age_adjustment`'s copy of the bug but not `age_adjustment`'s.
- **v3 (final):** both copies of the bug fixed. This is the list `canonical_project_state.md` §2 reflects.

Lesson: a surprising ranking shift is a signal to trace the actual mechanism before explaining it, not to pattern-match to the most recent change.

---

## 2026-08-26

20. **Two prior notebooks audited for portable functions.** `Understanding_Fighter_Careers.ipynb` and `Opponent Similarity Algorithm.ipynb` (429 cells) reviewed for reusable logic. `create_title_bouts_datasets` was already ported verbatim in `ude_points_utils.py`; every item below traces back to one of these two notebooks.

21. **`create_fighter_status_dataset` ported**, with an `as_of` parameter added beyond the original — the notebook version always compared against `datetime.now()` with no way to get a reproducible answer across runs taken on different days.

22. **Weight-class/era ranking rebuilt, not ported as-is.** The notebook's own local reimplementation of `rank_fighters_by_latest_ude_points` keyed its results dict by fighter *name* rather than `fighter_url` — two different fighters sharing a name would silently collide. It also built on `rank_fighters_by_latest_ude_points` (cumulative last-fight total), which this project's own history (see "Locked GOAT ranking" above) already established is volume/recency-biased relative to `rank_fighters_by_shrunk_ude_rate`. Rebuilt as `rank_fighters_by_shrunk_ude_rate_by_weight_class`: `filter_by_weight_class`/`filter_by_year` pre-filter, then the real ranking function runs unchanged, with the shrinkage target recomputed on the filtered (division-scoped) population.

23. **`calculate_overall_potency`'s ratio construction was a real bug, fixed.** The notebook version computed `ko_tko_wins / sig_strikes_landed` and `submission_wins / (td_landed + sub_att)` — a fight-level count divided by a strike/action-level count — then min-max normalized the result. Confirmed the failure concretely: Diego Lopes scored a 25.0 "grappling potency" from a single submission win over just 4 career grappling actions. Replaced with `finishes / wins` (same units) shrunk toward each division's own finish-rate baseline (`_shrink_rate`, same shape as `shrunk_win_rate`). Also fixed a stale column reference (`method_details`, which the current pipeline renames to `details`) that would have crashed outright on today's schema.

24. **`calculate_striking_power` added — new metric, "who hits hardest."** `kd / head_strikes_landed`, not win-conditioned (unlike potency), shrunk toward each division's own baseline, injury stoppages excluded. Sensitivity-checked: top-10 rankings hold 9–10/10 stable across a `prior_strength` sweep of 1→30 (potency's equivalent sweep: 7–10/10 — power is sturdier because its floor, `min_head_strikes_landed`, gates the ratio's own denominator directly, rather than a looser fight-count proxy).

25. **Investigated why Khalil Rountree Jr. outranks Alex Pereira on `calculate_striking_power` at LHW, despite Pereira's public "hardest hitter" reputation.** Not a bug: Khalil's raw rate (14 kd / 338 head strikes = 4.14%) genuinely exceeds Pereira's (5/204 = 2.45%) before shrinkage does anything. Traced to two mechanisms: (1) `kd` is a discrete UFCStats box-score event, not a continuous power measurement — Pereira's Oct 2025 KO of Ankalaev shows `kd=0` despite ending the fight by strikes; (2) the metric isn't win-conditioned by design, so Khalil's larger 17-fight sample (including decisions and losses where he still scored knockdowns) accumulates more numerator credit than Pereira's shorter, faster-finishing 7-fight LHW sample can. Tested whether a per-minute-of-cage-time framing (rewarding speed-to-finish) would flip the order — it didn't (Khalil still leads, 1.27 vs. 0.91 raw KD/15min); the residual gap against public perception is most likely Pereira's kickboxing record (outside this dataset entirely) and finish-quality/visual-drama factors this dataset can't see, not a defect in the metric.

26. **Injury filter added to `calculate_striking_power`** for consistency with `calculate_striking_potency`/`calculate_grappling_potency` (both already excluded injury-cause stoppages via `details`; power didn't). Effect confirmed small but real: 81/8,564 fights in the dataset have "injury" in `details`; power's own top-5s per division were unaffected except FLW, where the ordering shifted slightly among the top handful.

27. **Rematch analysis fixed and ported.** Three dormant edge-case issues found in the notebook version, none live in the current dataset but all fixed rather than left as known gaps: (1) `filter_immediate_fighter_rematches` hardcoded an `is_rematch == 0` check that's only correct for a first rematch, not generic over `rematch_type` as its own signature implied — never actually called with anything else, so dormant; not ported, superseded by routing through the fully general `process_rematch_data`/`is_immediate_rematch` instead. (2) `find_same_winner_rematches`'s winner-comparison returned `None` for both draws and no-contests, so two draws between the same pair would spuriously compare equal — checked, 0 fighter pairs currently have 2+ draws against each other (61 draws total), dormant. Fixed with distinct `'draw'`/`'no_contest'` sentinels. (3) `filter_invalid_rematches` detected a no-contest via `method_mapped in ['Overturned', 'Could Not Continue']` rather than checking `fight_result == 'NC'` directly — checked, currently 89/89 NC-result fights in the dataset do have one of those two method values, so coincidentally redundant today, not guaranteed by the check itself. Fixed by reusing `ude_points_algorithm.is_no_score_fight` (also now correctly covers `DQ`, which the original never checked at all).

28. **Opponent-durability adjustment — two real, live bugs found and fixed, then a design gap closed on top.** The notebook's "adjusted" KO efficiency divided a fighter's raw stat by their opponent's raw historical vulnerability rate, patched for the zero/missing cases with a hardcoded `0.0005` floor. Measured directly against the live dataset before fixing: 56.6% of all fighter-fight observations hit the `==0` case (opponent had never been finished standing before) — not a rare edge case, the *majority* case, since "ever been finished standing" is much rarer than "ever absorbed a strike" — each divided by the arbitrary `0.0005`, an unbounded up-to-2000x multiplier; a further 15.4% had a `NaN` opponent-vulnerability value that silently propagated to `NaN` through the division — the exact same bug class as the `_age_multiplier` NaN issue (#11 above). Rebuilt as `add_opponent_durability_multiplier`: Bayesian-shrunk toward a population baseline (same `_shrink_rate` machinery as potency/power) and bounded to `[0.5, 2.0]`, matching how every other multiplier in this project is bounded. Initial version used one dataset-wide baseline; checked and found miscalibrated in a predictable, division-specific direction (standing KO/TKO-loss rate per strike absorbed spans an 8.4x range, HW 0.64% to WSW 0.076%, against a 0.33% global baseline sitting in the middle) — fixed to shrink toward each fight's own weight-class baseline instead (falling back to the dataset-wide rate below `DURABILITY_MIN_DIVISION_OBSERVATIONS`, same "insufficient prior history → broader fallback" pattern as `_build_temporal_calibration_cache`). Cross-division fix confirmed correct (Khalil Rountree Jr. moved from #4 to #3 in LHW's durability-adjusted ranking; HW/FLW top-5s held steady) but did NOT reduce the overall bound-hit rate (52.1% at ceiling both before and after) — investigated and determined this residual is a structural property of shrinking a right-skewed rare-event rate toward its mean (most fighters legitimately sit below the mean for a rare event), not a miscalibration. Considered and rejected switching to median-based shrinkage: no clean Bayesian formulation exists for it, and it would introduce a second, inconsistent shrinkage philosophy into the file for unclear benefit. Left as mean-based; revisit only if a specific downstream need for finer discrimination among highly-durable opponents arises.

29. **Weight-class KO-rate significance test redone properly.** The original notebook's ANOVA/Kruskal-Wallis ran on a per-fight variable (`1/(strikes landed in that fight)` for a KO, else 0 — zero-inflated, not the same statistic as the population-level "total KOs / total strikes" ratio the headline claim was actually about), and ANOVA's normality assumption was checked against a *different* variable entirely, never the one the test was actually run on. Redone as a Poisson GLM with `log(strikes landed)` as an exposure offset and weight class as the predictor — this directly models the claimed quantity. Result: weight class is a highly significant predictor overall (likelihood-ratio test vs. intercept-only, χ²=470.1, df=11, p=7.5×10⁻⁹⁴); HW converts strikes to KO/TKO finishes at 2.47x FW's rate (95% CI 2.10–2.91x, p=9.2×10⁻²⁸) — a stronger, better-supported version of the original's own ~1.45x estimate, now with a real confidence interval.

30. **STANCE added to the pipeline and the dataset regenerated.** `dataset_processing_pipeline.py`'s `bio_cols` (step 6) didn't carry the raw fighter-bio scrape's `STANCE` field through, even though `process_fighter_bio` already cleaned it (`'--'` → `NaN`) — an omission, not a design choice; user confirmed the raw `fighters_df` does have the field. Added `'STANCE'` to `bio_cols`. Ran the full ETL against the project's original raw inputs (a pre-UFC-330 historical snapshot + the UFC 330 card + the fighter-bio scrape) and confirmed the reconstructed 8,564-fight raw dataset matches `fights_up_to_islam_garry_ready_for_features.csv` on every shared column except a benign DOB string-format difference for the 12 new fights (`"Oct 30, 1986"` vs. the correctly re-standardized `"1986-10-30"` — `calculate_age`'s own documented behavior). The ETL's bio merge only covers freshly-scraped rows, so historical fighters' STANCE was separately backfilled from the same fighter-bio file by `fighter_url` (stance is a largely time-invariant, roster-wide fighter property, not something that needs re-scraping per fight) — coverage went from 12 fights to 16,750/17,128 fighter-fight observations (97.8%). Ran the full pipeline (`engineer_all_features` → `calculate_ude_points_with_ablation` → `add_ude_points_difference_columns`) and confirmed the result is bit-identical to the locked `v2_6.csv` on all 255 shared, non-`STANCE` columns across all 8,564 fights — promoted as `latest_fights_up_to_islam_garry_with_ude_points_calculated_v2_6_with_stance.csv`.

31. **`ordered_columns` whitelist fixed from drop-unlisted to append-unlisted**, closing the exact failure mode that silently dropped `STANCE` even after #30's bio merge succeeded. By the final column-ordering step, every column present was already deliberately constructed by an earlier step — there's no legitimate junk left to filter at that point, so the list's real job is establishing a readable order, not deciding what belongs in the output. Switching it immediately surfaced a second, unrelated pre-existing bug: `convert_to_one_fight_one_row`'s `.groupby('fight_url').nth(0)`/`.nth(1)` calls don't drop their pre-groupby row index, so a bare `.reset_index()` was creating a meaningless `index_fighter_1`/`_fighter_2` column that the old whitelist had also been silently swallowing, unnoticed, this whole time. Fixed both: `ordered_columns` appends unlisted columns instead of dropping them; `.reset_index(drop=True)` added to both `.nth()` calls. Re-verified the full pipeline output unchanged (0 mismatches against the already-validated #30 run) after both fixes.

32. **Opponent-similarity algorithm ported, redesigned, and two real bugs fixed.** Adapted from `Opponent Similarity Algorithm.ipynb`'s "Algorithm to find best match to current opponent" section. Two confirmed bugs in the original: physical-similarity columns (`'Height (m)'`, `'Reach (in)'`) were silently excluded from every comparison the notebook actually ran, because its default column selector matched lowercase keyword substrings (`'height'`, `'reach'`) against capitalized column names — `'height' in 'Height (m)'` is `False` in Python; and age/stance were stated as intended comparison dimensions in the notebook's own markdown but never actually captured anywhere in the profile-building code. Redesigned per discussion: physical (age, height, reach) and style (`dynamic_*` accuracy/defense) similarity kept as two separate outputs rather than one blended score (this project's established "non-redundant signals" principle — see `canonical_project_state.md` §1); raw differences min-max scaled before summing (the original summed unscaled differences across columns on wildly different numeric ranges, so whichever column had the largest range dominated regardless of actual significance). A new bug caught during testing, not carried over from the notebook: a past opponent with zero comparison data was scoring a false "perfect match" (`total_difference = 0.0`) because missing values were filled to 0 before summing rather than excluded — fixed by averaging only the columns actually present, so a fully-missing row now correctly sorts to the bottom (`NaN`) instead of the top. `exclude_future_opponent=True` (default) added afterward, since a fighter who's already fought their upcoming opponent trivially (and unhelpfully) ranks that real fight as "most similar" to itself. Stance wired in last, once #30 made real stance data available — surfaced as a `stance_match` flag (`'same'`/`'different'`/`'unknown'`), not folded into the numeric distance, since it's categorical. One spot-check during testing at the time flagged Thiago Santos's `stance_match` value (Orthodox) as suspect, on an unverified assumption that he's a southpaw — **that assumption was wrong, not the data.** A parallel content-strategy session checked UFCStats.com directly and found his listed stance genuinely is Orthodox; re-verified here independently against the live page (`fighter-details/dea070ed4a2a8281`) before correcting this entry — confirmed, `STANCE: Orthodox`. No data-quality issue ever existed for this fighter; the general principle (spot-check individual stance values against UFCStats.com before publishing, since coverage is ~98% not 100%) still stands, but this specific example doesn't illustrate a problem and shouldn't be cited as one. Lesson: an unverified claim about what a public figure's stance "is known to be," made from memory rather than a checked source, carries exactly the same risk this project already guards against for its own dataset -- see `MEMORY.md`'s verify-before-ready rule.

---

## 2026-08-27

33. **`decisive_wins`/`close_wins`/`ties` misclassification found and fixed in `calculate_phase_magnitude_and_pdi`.** User asked how these buckets are determined, then specifically whether "1 vs 0" takedowns/submission-attempts should really classify as decisive — it shouldn't, but was. Root cause: `_dominance_magnitude`'s "close" curve (used for takedowns and submission attempts) hits its 0.35 cap exactly whenever the opponent lands 0 of that stat, and the blend weight pulling toward "decisive" is a product of two sigmoids that mathematically approach but never reach exactly 0 — so even a landed count far below its `count_threshold` (e.g. 1 landed vs. 3.5 threshold for takedowns) leaks an infinitesimal decisive contribution, nudging the result a hair past 0.35 (`0.35000000000886` for 1-vs-0 takedowns, `0.35000019750` for 1-vs-0 submission attempts — both float noise 7-12 decimal places out, not real signal). Traced one more case while explaining the mechanism: 2-vs-0 submission attempts (threshold 2.5) evaluates to a genuinely non-negligible `0.3543`, not float noise — landing 2 is only half a count short of the 2.5 threshold, not 1.5-2.5 short, so the sigmoid hasn't decayed nearly as far.
    - Fixed at the classification step only, not inside `_dominance_magnitude` itself: rounded a separate copy of the five phase magnitudes to 3 decimals (matching `pdi_margin`'s own existing rounding precision in the same function) immediately before the `> 0.35` bucketing, leaving the raw magnitudes that feed `pdi_1`/`pdi_2`/`pdi_margin` completely untouched. Deliberate choice over fixing `_dominance_magnitude`'s formula directly: that function's output also feeds `dominance_adjustment`, `perf_scale`, and `method_pdi_residual_points` — the core of the locked v2.6 scoring engine — so changing its numeric output at all, even by float noise, would break bit-for-bit reproducibility against the locked dataset for no scoring benefit, since nothing in `ude_points_algorithm.py` or `ude_points_utils.py` actually reads `decisive_wins`/`close_wins`/`ties` (confirmed by grep, same as the `who_won_*`/`phases_won` narrative-only columns already documented in `canonical_project_state.md` §3).
    - Verified against the full dataset before promoting anything: `pdi_margin_fighter_1`/`_fighter_2` showed 0.00 max absolute difference across all 8,564 fights, confirming zero scoring impact. The classification fix itself touched far more fights than the "1 vs 0" example suggested — 2,242 fights on the fighter_1 side, 2,108 on fighter_2's (asymmetric because a phase flipping from decisive-win to close-win for one side is simultaneously a flip from decisive-loss to close-loss for the other). Checked why the count was so much larger than expected: 100% of the 2,242 changed fights involve a takedown or submission-attempt shutout (one side landing ≥1, the other exactly 0) — the bug's trigger condition turns out to be common in real fight data, not rare, since one side landing zero takedowns or submission attempts happens constantly. `ties` didn't change in any fight (a magnitude of exactly 0 is untouched by rounding).
    - Regenerated `latest_fights_up_to_islam_garry_with_ude_points_calculated_v2_6_with_stance.csv` from the raw ETL inputs through the full pipeline (`run_etl_pipeline` → STANCE backfill → `engineer_all_features` → `calculate_ude_points_with_ablation` → `add_ude_points_difference_columns`) with the fix in place. Diffed old vs. new file column-by-column: only the 8 expected classification-count columns changed, in exactly the counts above; every other column (all UDE points, `pdi_margin`, every engineered feature, `STANCE`) bit-identical. Zero unexpected mismatches.

---

## 2026-08-31

34. **Stance field renamed `STANCE_fighter_1`/`_2` → `Stance_fighter_1`/`_2`.** Inconsistent with the Title Case naming of the other derived bio fields (`Height (m)`, `Weight (lbs)`, `Reach (in)`) — those are new columns `process_fighter_bio` computes from the all-caps raw scrape fields; `STANCE` needed no computation, so it had been passed through under its raw, all-caps scrape name instead of being given the same Title Case treatment. Fixed by having `process_fighter_bio` expose the cleaned raw `STANCE` field as a new `Stance` column, and updating `bio_cols`/`ordered_columns` in `run_etl_pipeline` and the two column-name references in `ude_points_utils.py`'s opponent-similarity code (`generate_fighter_profile`, `calculate_similarity_differences`) to match. Regenerated `..._v2_6_with_stance.csv` end-to-end and diffed against the prior file (after accounting for the rename): zero value mismatches anywhere — purely a column-name fix.

35. **`current_df_with_stance.csv` produced as a standalone deliverable.** User needed the (pre-UFC-330) `current_df.csv` snapshot itself to carry `Stance_fighter_1`/`_2`, independent of the full `..._v2_6_with_stance.csv` pipeline output. Backfilled directly from `fighters_df.csv` by `fighter_url_fighter_1`/`_2`, same approach as the main dataset's stance backfill: 17,058/17,108 (99.7%) coverage, matching the main dataset's rate exactly since the ~50 fighters missing stance in `fighters_df.csv` are the same population either way.

36. **Investigated whether the user's own fighter-bio scrape (`ufc_fighters_with_details_inc_dob.csv`, renamed to `fighters_df.csv` in this project) was actually necessary given a pre-existing public source (`ufc_fighter_tott.csv`, from greco1899's UFC-stats scraper on GitHub) already covers the same fighters.**
    - **Workflow context, for future reference:** the user's ongoing update process is: manually download the latest `ufc_fighter_tott.csv` from greco1899's GitHub → run it through their own local `fighter_scrape_new.py` → its output `ufc_fighters_with_details_inc_dob.csv` is what gets renamed to `fighters_df.csv` for this project. Separately, `current_df.csv` (the historical fight-level snapshot fed into `run_etl_pipeline` as `current_dataset`) gets renamed forward each time the dataset is brought current — it was `latest_fights_up_to_gamrot_salkilld` before this session's UFC 330 update, and the user intends to rename the current `latest_fights_up_to_islam_garry_with_ude_points_calculated_v2_6_with_stance.csv` to `current_df` for the next update cycle once ready.
    - **Coverage comparison, both files cover the identical 4,588-fighter URL set:** the user's scrape has equal-or-better coverage on every field, never worse except a single row each on two fields — Height +103 fighters, Reach +105, STANCE +89, DOB +265 (the standout gain, matching the file's "inc_dob" name), Weight net -1. Verdict: the scrape was genuinely necessary, not redundant — it materially improved coverage, most dramatically for DOB.
    - **Where both sources have a value, they don't always agree:** STANCE — 0 conflicts (perfect agreement wherever both are present). Height/Reach — 22 and 16 conflicts respectively, almost all off by exactly 1 inch (measurement/scrape-timing noise, not concerning). Weight — 73 conflicts, several large (e.g. Jailton Almeida 225 vs. 205 lbs, Joel Alvarez 170 vs. 155) — read as fighters who changed weight class between when each source's snapshot was taken (UFCStats' bio page shows current contracted weight, which moves over a career), not errors. DOB — 13 conflicts, one of which is not noise: Jose Montanha and Henrique Da Silva Lopes had their DOBs (and, in `ufc_fighter_tott.csv`, their names) swapped between the two sources.
    - **Verified directly against live UFCStats.com, not assumed:** `fighter-details/1f8cd7634e8286ff` is Jose Montanha (record 7-1-0, DOB Aug 25, 1996, with a real scored fight — a submission win over Louie Sutherland at UFC Fight Night: Gamrot vs. Salkilld, already in this project's dataset). `fighter-details/4e30f2761b6bd3b5` is Henrique Da Silva Lopes, **nicknamed "Montanha"** (record 5-2-0, DOB Mar 08, 1985, zero scored UFC fights). Two different real people who happen to share the name "Montanha" — one as an actual surname, one as a nickname — which is almost certainly what caused `ufc_fighter_tott.csv` to cross-assign their names and DOBs. The user's scrape has both DOBs correct, matching the live site exactly for both fighters; `ufc_fighter_tott.csv` has them swapped for both name and DOB.
    - **Net conclusion:** `fighters_df.csv` (confirmed byte-identical to the user's own scrape — see #30-era work) has been the correct source all along for this specific pair, and more generally has strictly better coverage than the alternative public source across every bio field. Nothing in the pipeline or `..._v2_6_with_stance.csv` needs correcting because of this; if anything, this is a second, independent confirmation (after the coverage numbers) that the scrape-it-yourself step in the user's update workflow is doing real, necessary work rather than duplicating an already-adequate public source.

37. **Corrected entry #32's Thiago Santos claim.** A parallel content-strategy session (its own `content_history.md`) independently checked UFCStats.com and found the claim ("shows as Orthodox... despite being publicly known as a southpaw") didn't hold up. Re-verified directly against the live page (`fighter-details/dea070ed4a2a8281`) before touching anything: `STANCE: Orthodox`, confirmed. The original claim was an unverified assumption made from memory, not from a checked source — exactly the failure mode the project's own verify-before-ready rule exists to catch, just made about a public fact instead of a dataset column. No data-quality issue ever existed for this fighter; entry #32 corrected in place with this noted, rather than left standing.

38. **`validate_transformed_data` extended, and a new `validate_dataset_regeneration` added, both in `dataset_processing_pipeline.py`.** Two gaps, both closed:
    - `validate_transformed_data` runs on the freshly-scraped batch BEFORE `current_dataset` is merged in (`run_etl_pipeline` step order), so its existing Primary Key Uniqueness check could never catch a fight that's genuinely new to this batch but already exists in `current_dataset` (e.g. an incremental-fight-filter bug upstream re-including something already processed). Added an optional `current_dataset` parameter and a 6th check: raises if any of the batch's `fight_url` values are already present in `current_dataset`. Tested against real data both ways: a normal run with genuinely new fights passes; re-running with the same batch against a `current_dataset` that now includes those fights correctly raises, naming the 12 offending URLs.
    - Formalized the ad hoc "diff the old and new file" checks this project has run by hand after every regeneration this session (STANCE casing, `ordered_columns`/`reset_index`, the classification-rounding fix, the UFC 330 stance addition) into `validate_dataset_regeneration(old_df, new_df, key_col='fight_url', columns_expected_to_change=None)`. Deliberately a separate function from `validate_transformed_data`, not folded into it: `validate_transformed_data` runs on raw ETL output before feature engineering or scoring exist yet, so it structurally cannot see `pdi_margin` or any UDE point; a regeneration diff needs to run on the FINAL, fully-scored output instead, which is a different pipeline stage with different available data. Raises on any lost `fight_url` regardless of the allowlist (a missing fight is never an acceptable "expected change"), and on any changed column not explicitly listed as expected — an unexpected column changing during a routine refresh is exactly the failure class this project has hit multiple times (a column silently dropped, a stray column silently created, a fix's blast radius wider than predicted). Tested against the real production file: self-diff clean, an allowlisted change passes, an unlisted change raises, and a dropped row raises regardless of the allowlist.
    - While testing, found the production file had been renamed externally (`..._v2_6_with_stance.csv` → `..._v2_6_final_with_stance.csv`) since it was last touched in this conversation — confirmed the data itself is unchanged (same 8,564×258, correct `Stance` columns) before updating `canonical_project_state.md`'s reference to match. Historical entries above that reference the old name are left as-is, since they accurately describe what the file was called at the time.

39. **Productionization planned (GitHub Actions, PR-gated) and the first build step landed: `fighter_scrape_new.py` made incremental.** Full workflow read directly from the two scraper scripts the user shared (`fighter_scrape_new.py`, `ude_scrape_new.py`) rather than assumed: `ude_scrape_new.py` already filters to only fights not already in the current dataset before scraping; `fighter_scrape_new.py` did not have an equivalent -- it re-scraped the entire ~4,600-fighter roster from scratch on every run, with no delay between requests and no retry on a failed page.
    - Design: a fighter is (re-)scraped if new (not in the existing roster at all), active (appeared in this run's freshly-scraped fights -- the trigger for Weight specifically, since Height/Reach/Stance/DOB are permanent but UFCStats' Weight field reflects current contracted division, which moves when a fighter changes weight class, and the most reliable sign that's become visible on their bio page is that they just fought), or incomplete (any bio field still blank from a prior run, in case UFCStats has since filled it in). Everyone else -- known, inactive, complete -- is skipped.
    - Rebuilt as `determine_urls_to_scrape`/`merge_scraped_results`/`run_fighter_scrape`, CLI-driven via argparse instead of hardcoded paths, with the same batch-scraping mechanics as before plus retry-with-backoff on `scrape_fighter` (matching `ude_scrape_new.py`'s already-proven pattern) -- worth having now that this is meant to run unattended in CI rather than under a human who'd just re-run failures by hand. `merge_scraped_results` keeps a fighter's prior data intact if their re-scrape attempt fails, rather than overwriting good data with a blank row.
    - The `playwright` import was moved from module load time into `start_browser()` specifically so the pure selection/merge logic could be tested without a browser dependency installed at all -- confirmed necessary: this sandbox doesn't have `playwright` installed, so testing would have been blocked entirely otherwise (`bs4`/`tqdm` were already present).
    - Tested directly (synthetic roster, since running the real scrape here would mean hitting the live site, which wasn't asked for at this step): correct selection (new + active + incomplete scraped, known-complete-inactive skipped) confirmed against a 6-fighter synthetic case; merge confirmed to update re-scraped fighters, add new ones, leave untouched ones alone, and preserve a fighter's prior data when their re-scrape attempt fails rather than blanking it; cold start (no existing roster at all) confirmed to fall back to scraping everyone, matching the original script's behavior.
    - Full plan, in build order: (1) this step, done; (2) parameterize `ude_scrape_new.py` the same way (it's already incremental, just needs CLI args instead of hardcoded paths); (3) a new `run_refresh.py` driver chaining acquisition → `run_etl_pipeline` → Stance backfill → `engineer_all_features` → `calculate_ude_points_with_ablation` → `add_ude_points_difference_columns` → `validate_dataset_regeneration`; (4) a GitHub Actions workflow (sketched, not yet placed in `.github/workflows/`) that runs on a schedule, fails the job outright if validation raises, and otherwise opens a PR with the refreshed files and the validation summary as its description -- PR review is the deliberate human-confirmation gate, chosen over a direct push to `main`.

40. **Productionization build steps 2 and 3 completed: `ude_scrape_new.py` parameterized, `run_refresh.py` built — and a real bug found and fixed in `validate_dataset_regeneration` (#38) while proving it end-to-end.**
    - `ude_scrape_new.py` was already incremental (`filter_new_fights_set` already skips fights already in the current dataset) — it only needed the hardcoded-path treatment: `latest_fights`/`current_dataset` reads and the exclusion-list filter moved into functions/a `run_fight_scrape(...)` entry point driven by argparse, `scrape_fight_details_to_csv` given an explicit `current_dataset` parameter instead of reading a module global, and the same lazy `playwright` import as `fighter_scrape_new.py` (#39) so the pure `filter_new_fights_set`/`exclude_dataless_events` functions are importable and testable without a browser dependency. Verified: both functions tested directly against synthetic data (an old excluded event correctly dropped, already-processed fights correctly skipped); both scraper files confirmed to still `py_compile` cleanly after the refactor.
    - `run_refresh.py` is the new single entry point: loads the four inputs, runs `run_etl_pipeline` → Stance backfill → `engineer_all_features` → `calculate_ude_points_with_ablation` → `add_ude_points_difference_columns` → `validate_dataset_regeneration`, writes the result, and exits non-zero if validation raises. Takes `--current-dataset` (the raw/ETL-stage historical snapshot, e.g. `current_df.csv` — has no `pdi_margin` or UDE points at all) and `--previous-production-file` (the prior *fully-scored* output) as two genuinely different arguments, not one — a regeneration diff can only mean anything against the fully-scored file, which doesn't exist until after this script's own pipeline runs, so it can't be the same thing `run_etl_pipeline` appends onto.
    - **Ran end-to-end against the real historical inputs already used to build the current production file** (same `current_df.csv`/`fighters_df.csv`/`latest_fights_df.csv`/`latest_events_df.csv` from #30 onward) — this should reproduce that exact file with zero differences, and initially didn't: `validate_dataset_regeneration` reported 4,649 rows of `dominant_fighter` "changing" between the two runs. Investigated before assuming either a real regression or trusting the new validator — found the freshly-computed `dominant_fighter` value for "no clear dominant fighter" is Python `None`, while the same value read back from a CSV that already went through a save/load cycle is `NaN`; the validator's `.astype(str)` comparison saw `"None" != "nan"` and flagged it as a value change. 100% of the 4,649 "differences" were this exact pattern — confirmed via `pd.isna()` on both sides before writing anything up as a finding.
    - Fixed at the actual source: `validate_dataset_regeneration`'s non-numeric branch now explicitly excludes rows where both sides are null (any representation) from being counted as differing. While fixing it, found a second, worse latent bug in the *numeric* branch this same investigation should have been checking: `(a - b).abs() > 1e-9` is `NaN` (not `True`) whenever either side is `NaN`, and `NaN > threshold` is always `False` in pandas/numpy — meaning a real regression where a numeric column's value silently disappeared into `NaN` (or vice versa) would have passed validation completely undetected. Fixed by explicitly OR-ing in an "exactly one side is null" check for the numeric branch specifically (the non-numeric branch didn't need this, since a real null-to-value change already produces different strings on the two sides).
    - Re-verified the full `validate_dataset_regeneration` test suite (the four cases from #38, still passing) plus three new cases targeting exactly this bug class: both-sides-null-different-representation correctly no longer flagged; a real numeric value silently turning into `NaN` correctly still raises; a real null-to-actual-value change in a string/object column correctly still raises. Re-ran `run_refresh.py` end-to-end after the fix: "No column-level differences on shared rows" — the driver script now correctly reproduces the existing production file bit-for-bit from the same inputs, confirming the whole chain (scrapers → pipeline → validation) works as designed.
    - Remaining: place the GitHub Actions workflow file in `.github/workflows/` (sketched, not yet done) and write `requirements.txt`.

41. **Productionization build finished: `.github/workflows/refresh_dataset.yml` and `requirements.txt` written.**
    - Corrected a real ordering bug in the original workflow sketch before finalizing it: `ude_scrape_new.py` must run BEFORE `fighter_scrape_new.py`, not after -- the fighter scraper's "active fighter" re-scrape trigger (#39) needs to know which fighters just fought, and that only exists once the fight scrape has produced `latest_fights_df.csv`. The original sketch had them in the wrong order, which would have made the active-fighter trigger a silent no-op every run (nobody would ever look "recently active" if the file it checks doesn't exist yet).
    - `requirements.txt`: checked against actual imports across every `.py` file rather than assumed -- `pandas`, `numpy`, `statsmodels`, `beautifulsoup4`, `playwright`, `tqdm` are genuinely used; `scipy` was in an earlier draft but isn't imported anywhere in the codebase (only in an ad hoc analysis script from earlier in this session, never part of the repo) and was removed.
    - Flagged, not resolved, directly in the workflow file's own comments: the workflow assumes `current_df.csv`/`fighters_df.csv`/the production CSV are committed to the repo and readable via `actions/checkout` -- this project's actual GitHub repo (checked directly, see #entry on the circular-import mix-up) does NOT currently contain these data files, only the `.py`/`.md` files. This needs a real decision (commit the CSVs, use Git LFS, or fetch them from wherever they actually live) before the schedule trigger is safe to enable -- not something to guess at silently.
    - Also explicit in the workflow's comments: it does NOT auto-rename the freshly-produced production file forward into `current_df.csv` for the next cycle. That's the step described as something done "when ready to update" -- left as a deliberate manual action rather than folding it into the same automated run that opens the PR, since merging a PR isn't necessarily the same moment as being ready to start the next refresh cycle.
    - Verified: the workflow YAML parses cleanly (checked with `yaml.safe_load`, 8 steps, correct `workflow_dispatch`/`schedule` triggers); all four `.py` files involved (`run_refresh.py`, `fighter_scrape_new.py`, `ude_scrape_new.py`, `dataset_processing_pipeline.py`) still `py_compile` cleanly with `requirements.txt`'s exact dependency list.

42. **Resolved the open "where do the data CSVs live" question from #41 -- and found `current_df.csv`/`--previous-production-file` are actually the same file, not two.** User pushed `current_df.csv` and `fighters_df.csv` to the GitHub repo; re-cloned and checked directly rather than trusting the filenames alone. `fighters_df.csv` matched expectations. `current_df.csv` did not: 8,564 x 258 columns, `pdi_margin`/`ude_points_post_fight`/`Stance` all present -- this is the fully-scored production file, not the ~231-column raw/ETL-stage snapshot `run_etl_pipeline`'s `current_dataset` argument was designed around (that file, used all session locally, has no scoring columns at all).
    - Not a mistake -- consistent with the project's own already-documented convention (canonical_project_state.md §7: the production file gets renamed forward to become the next `current_df.csv`). There is only one rolling file, not two: `current_df.csv` at any moment simply IS last cycle's finished output under a recurring generic name. `run_etl_pipeline` handles this safely either way -- it reduces `current_dataset` to only the columns shared with the fresh raw scrape before merging, regardless of whatever extra scoring columns the input happens to also carry.
    - Given that, no separate "production CSV" needed to be added to the repo at all. Updated `run_refresh.py`'s invocation in `refresh_dataset.yml` to pass `current_df.csv` for both `--current-dataset` and `--previous-production-file`, and rewrote the workflow's own top-of-file comment (which had said the opposite) to match. Re-verified the YAML still parses cleanly after the edit.

43. **First real GitHub Actions run: every actual pipeline step passed.** User triggered `workflow_dispatch` manually. Read the run directly (GitHub's Annotations panel is readable without signing in, even though full raw logs require it) rather than relying only on a pasted log: checkout, dependencies, the greco1899 fetch, both incremental scrapes, and "Run full pipeline + validation" (the whole `run_etl_pipeline` → Stance backfill → `engineer_all_features` → `calculate_ude_points_with_ablation` → `add_ude_points_difference_columns` → `validate_dataset_regeneration` chain) all succeeded against real UFCStats.com data. Only the final "Open PR" step failed: `Error: GitHub Actions is not permitted to create or approve pull requests.` -- a repo-level policy gate (Settings → Actions → General → Workflow permissions) that overrides whatever `permissions:` the workflow YAML itself requests; confirmed this can't be fixed from the YAML alone for that specific error, though using a PAT instead of the default `GITHUB_TOKEN` is a legitimate YAML-side workaround if the repo setting is genuinely inaccessible. User's pasted log also showed the PR branch (`dataset-refresh/<run_id>`) was created and pushed successfully before the PR-creation API call itself was rejected -- an orphaned branch, harmless, worth deleting manually.

44. **Fixed the real scraper-efficiency issue the first run surfaced: the "incomplete profile" trigger had no retry limit.** "Scrape fighter bios (incremental)" took ~9 of the run's ~11 minutes -- large for a step meant to only touch a small delta. Root cause: 1,994 of 4,588 fighters (43%) are missing Reach specifically, mostly older/retired fighters UFCStats never measured and never will -- a permanent gap, not a temporary one. As designed (#39), the "incomplete" trigger had no way to distinguish permanent absence from temporary absence, so it would re-visit that same ~2,000-fighter set every single run, forever.
    - Fixed with a persisted `bio_scrape_attempts` counter and `MAX_INCOMPLETE_RESCRAPE_ATTEMPTS = 3` cap in `fighter_scrape_new.py`: a fighter is only eligible for the "incomplete" trigger while their attempt count is below the cap. The counter increments on ANY scrape (new, active, or incomplete-triggered) that still leaves a field missing afterward -- a real attempt is a real attempt regardless of why the page got visited -- and resets to 0 the moment a profile becomes complete, so a field that genuinely does get filled in later doesn't stay artificially capped.
    - Backward compatible with the `fighters_df.csv` already sitting in the repo, which predates this column entirely: `determine_urls_to_scrape` treats a missing `bio_scrape_attempts` column as "everyone gets their first tracked attempt," not as an error or a full skip.
    - Tested directly: a fighter simulated as permanently missing Reach across 5 consecutive runs is scraped on attempts 1-3 and correctly excluded from attempt 4 onward; a field that does get filled in resets the counter to 0; a roster with no `bio_scrape_attempts` column at all (the current real-world case) still works correctly. Re-ran the full pre-existing test suite (new/active/incomplete selection, merge behavior including failure-preserves-old-data, cold start) to confirm nothing regressed -- all still pass. `py_compile` clean.

45. **User fixed the repo-level PR-permission setting (#43) and pushed the #44 `fighter_scrape_new.py` fix to `main`.** Before re-running, user flagged an apparent anomaly while comparing the orphaned `dataset-refresh/33347704123` branch (from the failed #43 run) against `main`: GitHub's compare view showed 13,433 additions and 4,589 deletions on `fighters_df.csv` alone -- suspicious for a run whose own log said only a few thousand fighters were even touched. Investigated by fetching the branch and diffing the actual file content directly rather than trusting the summary line. Root cause: `main`'s existing `fighters_df.csv` had Windows CRLF line endings on all 4,589 lines (confirmed via `grep -c $'\r'`); the freshly-regenerated file (written by `pandas.to_csv()` on the Linux Actions runner) had plain LF on all lines. A line-based diff can't see past the invisible trailing `\r`, so it flagged every single line as "changed" even where the underlying data was byte-identical. Re-diffed with line endings normalized out: the real change was 26 new fighters added plus a small number of previously-incomplete profiles genuinely filled in by a successful re-scrape (e.g. a fighter going from all-blank bio fields to a complete Height/Reach/Stance/DOB row) -- zero real data loss anywhere. Branch confirmed superseded and safe to discard once a fresh run supersedes it.

46. **Re-run succeeded end-to-end; first real PR merged.** User re-triggered `workflow_dispatch`, reviewed the validation report, and merged the resulting PR (`Merge pull request #1 from OsyTheDataGuy/dataset-refresh/33350742987`). Verified directly by re-cloning the repo: `fighters_df.csv` on `main` now has zero CRLF line endings (`grep -c $'\r'` returns 0), confirming the #45 line-ending mismatch was a one-time artifact of the file's earlier Windows origin and won't recur -- every future write goes through the same Linux/pandas path. User then manually renamed the newly-produced production file forward to `current_df.csv`, closing out the cycle per the (at the time) still-manual step documented in #41.

47. **The manual rename-forward step (#41, #46) automated per user request.** `run_refresh.py`'s `--output` in `.github/workflows/refresh_dataset.yml` now points directly at `current_df.csv` -- the same path already used for `--current-dataset` and `--previous-production-file` -- instead of a separately-named production file that then needed copying over by hand. Safe because `run_refresh.py` already loads both of those inputs fully into memory before writing anything (`pd.read_csv` calls happen before the single `final.to_csv(output_csv, ...)` at the end); confirmed this same-path read-then-overwrite pattern directly with a standalone test before sending the change, rather than relying on that being generally true. The PR's `add-paths` was updated to list `current_df.csv` in place of the old long filename, which is no longer produced at all.
    - This deliberately reverses a design choice from #41: previously, merging a PR and promoting its data to the next cycle's baseline were two separate moments, on the reasoning that a merged PR isn't necessarily the same moment as "ready to start the next cycle." User asked for that gap to be closed after seeing the manual rename step in practice (#46) -- merging a PR is now the same action as promoting it, with no remaining step in between.
    - User uploaded the change to `main` directly. Verified by re-cloning the repo: `refresh_dataset.yml` on `main` matches the sent version byte-for-byte. User also copied the latest `current_df.csv` into the local project folder for a separate content-strategy session's use; verified that copy is identical (`8,590 x 258`, `DataFrame.equals()` confirmed True) to what's currently on `main` -- genuinely the latest production file, not a stale local copy.

48. **`canonical_project_state.md`'s "Current Source Files" section (§7) audited against the real filesystem and found stale in four ways -- then rescoped, not just corrected, per user preference.**
    - Audit (not assumed accurate from the last edit): `find` confirmed `fights_up_to_islam_garry_ready_for_features.csv` and the old `..._v2_5.csv` snapshot, both described in §7 as present in the main folder, didn't exist anywhere in the project. `fighters_df.csv`, described as one of "the two live files" alongside `current_df.csv`, wasn't in the main folder either -- only a stale copy (4,588 fighters, no `bio_scrape_attempts`) sat in `ipynb and old datasets/`, out of sync with the repo's real 4,614-fighter version. That same subfolder held an undocumented stale duplicate of `current_df.csv` itself (8,554 x 231, pre-Stance, pre-automation) under the identical filename as the live production file -- a real landmine for anyone browsing by name. Separately, `ude_points_feature_engineering_pipeline.py`'s bullet claimed "Unchanged since v2.6's lock," which is false as a file-level statement: confirmed via mtime (Aug 27, six days after the Aug 21 lock) and content (`classification_magnitudes_1` from #33's fix is still there) -- the scoring *output* is unaffected, but the file itself was edited.
    - User deleted `ipynb and old datasets/` entirely and moved a fresh `fighters_df.csv` into the main folder; re-verified both changes directly (folder gone, `fighters_df.csv` in main folder byte-identical to the repo's copy) before touching the doc.
    - Per explicit user instruction, §7 was rescoped rather than just corrected: `dataset_processing_pipeline.py`, `ude_points_feature_engineering_pipeline.py`, `ude_points_algorithm.py`, and `current_df.csv` -- the four files the user actually shares alongside this document -- keep full detail. Everything else (`ude_points_utils.py`, `fighters_df.csv`, both scrapers, `run_refresh.py`, `requirements.txt`, the workflow YAML) collapsed into one line each under "Also in the GitHub repo, not routinely shared here," cross-referenced to §4/§8 rather than repeated inline. The four now-nonexistent-file bullets were removed outright instead of marked stale. The feature-engineering bullet's false "unchanged" claim was corrected to distinguish "scoring inputs unaffected" from "file unedited."
    - One more dangling reference surfaced by the folder deletion: §2a's Locked GOAT Ranking table cited `latest_fights_up_to_islam_garry_with_ude_points_calculated_v2_6.csv` as its source -- that file lived in the deleted folder. Reworded to state the table was a fixed historical result no longer reproducible from anything on disk, pending the reproduction in #49 below.

49. **§2a's Locked GOAT Ranking table reproduced live against `current_df.csv`**, closing the gap #48 flagged. `rank_fighters_by_shrunk_ude_rate(df, prior_strength=10.0, min_fights=10)` run directly against the 8,590-fight production file: top-20 membership and order are unchanged from the old snapshot; qualifying pool grew 619 -> 623 as new fights pushed a few more careers past the floor. The tiny (third-decimal) movement in each Shrunk Rate value traces entirely to `population_mean_rate` shifting as 26 new fights joined the full population -- none of the top-20 fighters personally fought again. Table and surrounding text in `canonical_project_state.md` updated to cite `current_df.csv` directly, with a note to re-run this after future refreshes rather than treat it as fixed.
    - User asked, separately, why the project ranks on Bayesian-shrunk per-fight rate rather than raw cumulative career points -- answered with real numbers pulled from the data rather than the docstring's hypothetical: Dustin Poirier (32 fights) ranks #31 by cumulative `career_point_gain` but only #56 by `shrunk_rate`, since volume alone compounds his below-average 1.06 rate into a large total. At the opposite extreme, Frank Shamrock and Bas Rutten (1 fight each) post `raw_rate` ~=5.0-5.1 off a single data point -- higher than GSP's career rate, and just as untrustworthy. `shrunk_rate` resolves both by weighting `raw_rate` against `population_mean_rate` in proportion to `n_fights` vs. `prior_strength` (10.0, in equivalent-fights units): a 1-fight career reverts almost fully to the mean (Rutten: 5.04 -> -0.73), a 22-fight career barely moves (GSP: 7.04 -> 4.43). `population_mean_rate` itself is negative (~=-1.30) because 39.5% of all 2,555 scored fighters have 3 or fewer fights and average -2.43 -- most short UFC careers end on a loss. Shrinkage alone still isn't sufficient at the margin, confirmed with a live example: Shavkat Rakhmonov (7 fights) would rank #37 overall by `shrunk_rate` alone with no floor applied -- ahead of many 15+ fight veterans -- which is why `min_fights=10` exists as a hard floor on top of the shrinkage rather than relying on the prior alone. Full explanation, with these same numbers, added to `canonical_project_state.md` §4a.

50. **`rank_fighters_by_shrunk_ude_rate_by_gender` added to `ude_points_utils.py`**, mirroring the existing weight-class version. User asked how to get male/female (and BW/LW-style division) GOAT rankings; answered first with ad hoc filtering, then built it as a first-class function once asked.
    - `weight_class_cleaned` already prefixes every women's division code with `W` (`WSW`/`WFLW`/`WBW`/`WFW` vs. the plain men's codes) -- `map_weight_class`'s own existing convention, not something new. `filter_by_gender(df, gender)` filters on `MENS_WEIGHT_CLASSES`/`WOMENS_WEIGHT_CLASSES` accordingly; `rank_fighters_by_shrunk_ude_rate_by_gender` chains it with `filter_by_year` then the unchanged base ranking function, same pattern as the weight-class wrapper -- `population_mean_rate` is recomputed within the filtered gender population, not the full roster, for the same reason a division-scoped ranking recomputes its own baseline.
    - `'Catch Weight Bout'`/`'Open Weight Bout'` rows (85 fights total) aren't attributable to either gender and are excluded from both `'M'` and `'F'`, documented directly in the docstring -- confirmed this causes a small (usually one-fight) undercount for a fighter who had one of these bouts (Islam Makhachev: 18 fights in the male-only view vs. 19 unfiltered).
    - Verified the new function reproduces the earlier ad hoc male/female results exactly (Georges St-Pierre still #1 male at `shrunk_rate` 4.427, Amanda Nunes #1 female at 2.525) before treating it as done. `py_compile` clean.
    - Checked every division's qualifier count at `min_fights=10` before presenting per-division rankings: all eight men's divisions and three of four women's divisions return a healthy pool (16+ qualifiers); WFW (women's featherweight) returns zero -- only 30 total fights exist in that division, too new/small for the standard floor. Flagged rather than silently worked around.
    - Produced top-10-by-`shrunk_rate` tables for all eight men's divisions against `current_df.csv`. Notable cross-checks from the real output: Dustin Poirier appears in both FW (#8) and LW (#7) rankings independently (expected -- `filter_by_weight_class` doesn't require a fighter to pick one "primary" division); Randy Couture appears in both LHW (#4) and HW (#4) for the same reason.

51. **Real bug found and fixed in `rank_fighters_by_shrunk_ude_rate`'s `career_point_gain`, surfaced by the user's own face-validity check on #50's division tables, not by any test this project had already written.** User flagged three things as suspicious: Brendan Allen outranking Whittaker/Adesanya at MW, Frankie Edgar outranking Jose Aldo at FW, and Sean Brady outranking Woodley/Edwards/Belal Muhammad at WW. Investigated all three against the raw per-fight ledger rather than assuming any of them was right or wrong.
    - Root cause, confirmed for MW and FW: `career_point_gain` was computed as the fighter's full-career UDE ledger value at their *last fight within the filtered subset*, minus 500 -- not "points earned in that division." Since the ledger runs continuously across a fighter's whole career, any fight in a *different* division sitting chronologically between two fights in the target division bled its points into that division's score. Concretely: Frankie Edgar's FW ranking (#3, 0.700) was almost entirely his old LW title run (including two wins over BJ Penn, +11.44/+9.32) showing up as "FW value" -- his real FW-only 13 fights rank him #32 of 50. Jose Aldo's FW ranking (#5, 0.480) was being dragged down by a net-negative BW stretch (2019-2024) sandwiched between his FW reign and a 2025 FW comeback fight -- FW-only, he's #2. Israel Adesanya's one LHW title loss to Jan Blachowicz (-13.92) was baked into his MW ledger, understating him relative to Robert Whittaker (unaffected -- an all-MW career) and Brendan Allen (also unaffected, also all-MW).
    - WW checked the same way and confirmed NOT a bug: Woodley, Edwards, Brady, and Belal Muhammad have all fought exclusively at WW, so no cross-division contamination is possible for any of them. Sean Brady's #3 ranking is real -- driven by a genuine result (beating Leon Edwards, +9.49) plus a clean 11-fight run with only two losses. Independently recomputing WW the corrected way barely moved anyone's position, exactly the signature expected if the original WW ranking was already correct.
    - Ilia Topuria's absence from the FW top 10 confirmed as the user's own hypothesis, not a separate issue: he has exactly 7 FW-specific fights, under the `min_fights=10` floor.
    - Fix: `career_point_gain` in `rank_fighters_by_shrunk_ude_rate` now sums `ude_points_diff_fighter_1`/`_2` directly from the rows actually present in the passed dataframe, rather than reading a ledger snapshot via `get_latest_ude_points_with_details`. Mathematically identical to the old method for the full, unfiltered population (a running ledger from a 500-point start IS the cumulative sum of every diff) -- verified directly: comparing old vs. new `career_point_gain` across all 2,555 fighters, 2,554 differ only by floating-point noise (~1e-14). Confirmed the base §2a ranking is unaffected: same top-20, same membership, changes only in the 6th decimal place of `shrunk_rate`.
    - One real (non-floating-point) exception surfaced by that same comparison: Kenichi Yamamoto, off by 7.56 points -- traced to a pre-existing data-quality issue unrelated to this fix, two 1999-11-19 "Open Weight Bout" rows (`fight_url` `9c7830c91fad60d8` and `a4e592c874e8233f`) that list him as fighting himself. He has only 3 career fights total, so it has zero effect on any `min_fights=10` ranking. Investigated and fixed in #52 (it turned out to be one visible symptom of a 26-row opponent-scramble in the legacy base ETL); the 7.56-point discrepancy is now resolved.
    - Both `rank_fighters_by_shrunk_ude_rate`'s and `rank_fighters_by_shrunk_ude_rate_by_weight_class`'s docstrings updated to describe the corrected behavior; the latter's previous note describing the now-fixed limitation as a known, accepted tradeoff was rewritten rather than left stale. `rank_fighters_by_shrunk_ude_rate_by_gender` inherits the fix automatically, since it calls the same base function.
    - Regenerated all eight men's-division top-10 tables with the fix. Visible, self-consistent ripple effects beyond the three fighters originally flagged: Randy Couture -- who famously moved back and forth between LHW and HW -- drops out of both divisions' top 10 entirely; BJ Penn, who also moved divisions (LW to WW), now appears in LW's top 10 (#10) where his real performance belongs; Frankie Edgar's genuine LW quality (not the FW ranking it was wrongly inflating) now shows correctly at LW #7.

52. **The "Kenichi Yamamoto fights himself" rows (flagged in #51) are not two isolated bad rows -- they are the visible tip of a systemic opponent-scramble in the legacy base dataset's ETL, affecting 26 rows across two event clusters. The public greco1899 source is not at fault.** Investigated the two `fight_url`s `9c7830c91fad60d8` and `a4e592c874e8233f` (both `event_date` 1999-11-19, both listing `fighter_1 == fighter_2 == "Kenichi Yamamoto"`).
    - **Ground truth, verified three ways and in agreement:** greco1899's `ufc_fight_details.csv` (URL->BOUT map), greco1899's `ufc_fight_results.csv`, and the live UFCStats.com fight-detail pages all say: `9c7830c91fad60d8` = Kenichi Yamamoto def. **Katsuhisa Fujii** (Ultimate Japan 2 Heavyweight Tournament Title Bout, Submission - Kneebar, R2 4:15); `a4e592c874e8233f` = Kenichi Yamamoto def. **Daiju Takase** (Open Weight Bout, Decision - Unanimous, R3). greco's per-round `ufc_fight_stats.csv` also has both fights' stats correctly attributed. The scrape source is clean.
    - **What `current_df.csv` actually contains for those two rows:** both rows carry `fighter_1` = Yamamoto's real stat line from the Takase fight and `fighter_2` = Yamamoto's real stat line from the Fujii fight -- i.e. neither opponent (Fujii or Takase) appears at all, both slots are Yamamoto, and the row metadata is a mix of bits from both fights (e.g. the `9c7830...` row has the Takase fight's `method` = "Decision - Unanimous" but the Fujii fight's `details` = "Kneebar From Bottom Guard" and `round_ended` = 2).
    - **A third UFC 23 row is corrupted the same way:** `ee1950e871aa5cc2` is really Katsuhisa Fujii def. **Masutatsu Yano** (Open Weight Bout, KO/TKO R2), but `current_df.csv` labels it "Katsuhisa Fujii vs. Daiju Takase", `HW`, "Submission", and fills it with Fujii's real stats *from the Yamamoto fight* plus Takase's real stats *from the Yamamoto fight*. The real Fujii-vs-Yano fight is absent from the dataset.
    - **Full blast radius, established by set-comparing every `current_df.csv` row's `{fighter_1, fighter_2}` against the greco1899 URL->BOUT map for all 8,590 rows:** 47 rows differ. ~21 are benign spelling/punctuation variants between the project's canonical fighter names and greco's (`Waldo Cortes-Acosta` vs `Waldo Cortes Acosta`, `Zachary Reese` vs `Zach Reese`, `Montserrat Rendon` vs `Montse Rendon`, `Rafael Cerqueira` vs `Rafael Cerquiera`, `Tre'ston Vines` vs `Treston Vines`, `Michael Aswell Jr.` vs `Michael Aswell`, `Jose Daniel Medina` vs `Jose Medina`) -- same person, same fight, not corruption. The remaining **26 rows are genuine opponent-identity corruption in two contiguous clusters:**
        - **UFC 19 through UFC 23 (1999-03-05 to 1999-11-19): 16 rows.** Within each affected card the loser identities are permuted across fights, so real fighters carry other fighters' names/URLs/stats. Examples: `b1134affa18449dd` (UFC 22, really Frank Shamrock def. **Tito Ortiz** for the LHW title) is stored as "Frank Shamrock vs. Masutatsu Yano"; `96c247ba0eb1ac65` (UFC 22, really Tim Lajcik vs. **Ron Waterman**, a draw) is stored as "Tito Ortiz vs. Tim Lajcik"; `997753de17f81e3a` (UFC 19, really Kevin Randleman def. **Maurice Smith**) is stored as "Kevin Randleman vs. Chuck Liddell". Affected `fight_url`s: `28f31c3fd7afe737`, `997753de17f81e3a`, `b26c2f616c9436fb`, `3a271d9d9210a818`, `73c07530f667289f`, `99b5010fd25911af`, `a3fe8217502c42a4`, `1db8bed8b4d30fb3`, `38e3986b7cfff608`, `df8d4014d8206ab1`, `62ac5bff3c181f50`, `96c247ba0eb1ac65`, `b1134affa18449dd`, `9c7830c91fad60d8`, `a4e592c874e8233f`, `ee1950e871aa5cc2`.
        - **UFC Fight Night: Holloway vs. The Korean Zombie (2023-08-26): 10 rows of 13.** Same signature -- winners' names/results/stats mostly intact, loser slot (name + `fighter_url` + stat line + DOB) taken from a different fight on the same card. E.g. `dd617f6ab4916ec4` (really Giga Chikadze def. **Alex Caceres**) is stored as "Ryan Spann vs. Giga Chikadze" with Ryan Spann's real stat line from his actual fight that night (the Anthony Smith fight, `c53b0f678fe2f16a`, which is itself stored correctly); `5531317352862978` (really Erin Blanchfield def. **Taila Santos**) is stored as "Fernie Garcia vs. Erin Blanchfield". Affected `fight_url`s: `23865dfc8361ac62`, `293c5f4c9aca5c54`, `48231fbd1af53674`, `5531317352862978`, `b65b37c0e00074d9`, `bfc9989751bd0d04`, `da6b863cf684e0ae`, `dd617f6ab4916ec4`, `de89bea7b0dcd2d3`, `f7bc845ce74f5fe7`.
    - **Where the defect lives:** not in greco1899 (verified above), and not in the current `dataset_processing_pipeline.py` -- these 26 rows sit in the legacy base file (`latest_fights_up_to_islam_garry_with_ude_points_calculated_v2_6_final_with_stance.csv`, and its predecessors) and were carried forward unchanged into `current_df.csv`. The current `ude_scrape_new.py` excludes UFC 19-23 by hardcoded event list, so a plain refresh never re-scrapes cluster 1; cluster 2 is already in the base, so the incremental scraper skips it too. The corruption is therefore frozen in place until the affected `fight_url`s are force-re-scraped. `scrape_fighters()` in the current scraper reads two distinct `a.b-fight-details__person-link` nodes and the live pages are now clean, so a targeted re-scrape of the 26 URLs would produce correct rows.
    - **Impact before the fix.** Cluster 1: every fighter involved has well under 10 total career fights, so no `min_fights`-gated ranking was affected (consistent with #51's note on Yamamoto) -- but Tito Ortiz's real UFC 22 title-fight loss to Frank Shamrock was missing entirely (replaced by a phantom win over Tim Lajcik), and Fabiano Iha's real UFC 20 debut loss was missing. Cluster 2 silently corrupted the per-fight record and UDE-point math for long-career, ranking-eligible fighters (Alex Caceres 30 dataset fights, Anthony Smith 25, Michal Oleksiejczuk 19, Ryan Spann 17 -- Spann's stat line was duplicated into the Chikadze fight, JJ Aldrich 17, Giga Chikadze 12, Chidi Njokuani 11, Song Kenan 11).
    - **Fixed same session: the 26 rows were force-re-scraped and the whole dataset rebuilt.** Re-scraped all 26 `fight_url`s directly from the live UFCStats fight-detail pages (same fields `ude_scrape_new.py`'s `scrape_ufc_fight` extracts), assembled them into the raw-scrape schema, removed the 26 corrupted rows from `current_df.csv`, and ran the exact `run_refresh.py` chain (`run_etl_pipeline` -> Stance backfill -> `engineer_all_features` -> `calculate_ude_points_with_ablation` -> `add_ude_points_difference_columns`) on the pruned file + the 26 fresh rows. `run_etl_pipeline` re-sorts by date and the feature/scoring stages are full-history chronological state machines, so every derived column (records, streaks, champion status, quality scores, PDI, UDE points) was recomputed for all 8,590 rows from the corrected inputs. Output written back to `current_df.csv` (8,590 rows, 258 cols, unchanged shape and `fight_url` set).
    - **Name normalization:** `update_fight_records`/streak/champion state machines key on the fighter *name string*, not the URL. One re-scraped fighter, `fc08099550072fe4`, comes off the live page as "Waldo Cortes Acosta" but `current_df.csv` stores him as "Waldo Cortes-Acosta"; the re-scraped rows adopt the name `current_df.csv` already uses for each `fighter_url` (falling back to the page spelling only for fighters who appear nowhere else -- Fujii, Yano, Marco Ruas, Bas Rutten, Frank Shamrock, Gary Goodridge, Andre Roberts, David Dodd, Travis Fulton, Andre Pederneiras, all legitimately single-fight in this dataset). Without this his record would have split in two.
    - **Verification.** Every structural/raw column (`bout`, `fighter_*`, `fighter_url_*`, `fight_result_*`, `method`, raw stat counts, DOB, `is_champion_*`, `title_defenses_*`) changes *only* on the 26 rebuilt rows; `is_champion_*` and `title_defenses_*` don't move at all. Record/streak changes outside the 26 (≈40 rows) are all later-career fights of the involved fighters and are all genuine corrections -- e.g. Fabiano Iha `1-1` -> `1-2` (his real Clark loss restored), Ryan Spann `9-7` -> `9-6` (phantom loss removed), Kenichi Yamamoto's pre-Miletich record `4-0` -> `2-0`. Five `is_rematch` flips outside the 26 are all correct consequences of fixing the opponents (Horn/Liddell and Clark/Iha become genuine rematches; Randleman/Liddell and Rizzo/Randleman stop being falsely flagged as one). ~3,700 further rows carry sub-0.02-point median drift in `method_pdi_residual` / `upset_rating_gap_smoothed` / `age_adjustment` -- the rolling 5-year (pre-2012: expanding) method/age calibration windows and the population rating baseline shifting slightly now that 26 fights are correct; `ude_points_diff` moves by >0.1 pt on ~110 non-rebuilt rows, max 1.34.
    - **The #51 Kenichi Yamamoto discrepancy is resolved:** his `career_point_gain` by the ledger method and by the direct `ude_points_diff` sum now agree exactly (both −1.40); the old 7.56-point gap is gone.
    - **Ranking impact.** GOAT §2a (`min_fights=10`) top-20: membership and order unchanged; Shrunk Rate values drift <0.002 (table + note in `canonical_project_state.md` §2a regenerated). Seven of eight men's divisions unchanged. Two reorders, both between fighters separated by <0.003 `shrunk_rate` (statistical ties): heavyweight #3/#4 Stipe Miocic ↔ Tom Aspinall, and bantamweight #1/#2 TJ Dillashaw ↔ Merab Dvalishvili.
    - **Not a code change** -- `ude_points_algorithm.py` / `ude_points_feature_engineering_pipeline.py` / `dataset_processing_pipeline.py` untouched; v2.6 scoring lock intact. A future routine `run_refresh` needs no special handling: the 26 corrected rows are now in `current_df.csv`, so `validate_dataset_regeneration` diffs clean against it going forward. The UFC 19–23 events remain in `ude_scrape_new.py`'s hardcoded exclusion list, so a plain refresh will not re-scrape or re-touch cluster 1.
