# Numerical stack pinned exactly. current_df.csv is regenerated from scratch and
# diffed against the committed copy on every refresh (run_refresh.py ->
# validate_dataset_regeneration). The method-PDI binomial GLM (statsmodels, via
# scipy/numpy) and the age IRLS fits are numerical optimisers whose output moves
# in the 3rd-4th decimal across library versions -- unpinned, CI drifts away from
# the committed baseline every time any of these releases, and the refresh fails
# validation before it can open a PR. Re-pin deliberately (and regenerate the
# baseline) when bumping any of these.
pandas==2.3.3
numpy==2.3.5
scipy==1.16.3
statsmodels==0.14.5
patsy==1.0.1

# ude_points_utils.py imports these at module level (not just where plots are
# drawn), and ude_points_feature_engineering_pipeline.py now imports from
# ude_points_utils inside the scoring path (add_dynamic_td_accuracy /
# add_dynamic_td_defence -> _shrink_rate) -- so engineer_all_features can't run
# without them, on a headless CI runner or anywhere else. None affect scoring
# output; pinned only for a reproducible install.
matplotlib==3.10.6
seaborn==0.13.2
plotly==6.3.0

# Scrape-only; do not affect scoring reproducibility.
beautifulsoup4==4.13.5
tqdm==4.67.1
playwright
