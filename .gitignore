# OS/editor cruft
.DS_Store
__pycache__/
*.pyc

# Large one-off backup snapshots of current_df.csv -- not meant to be
# version-controlled (git isn't built for tracking large data snapshots,
# and current_df.csv itself is already the authoritative, git-tracked copy).
# Every backup snapshot, current and future, belongs in backups/ -- nothing
# under it should ever be committed.
backups/

# CLAUDE.md governs Claude sessions working in this local folder -- it stays
# local forever, never pushed to the remote repo.
CLAUDE.md

# age_calibration_validation.md is the one-time backing doc for the age
# recalibration (referenced from canonical_project_state.md and
# ude_points_algorithm.py). It's committed once as a frozen reference and not
# maintained -- ignore local edits so it doesn't reappear as an untracked
# change. canonical_project_state.md is the living source for anything current.
age_calibration_validation.md

# Commercial goals / monetization plan -- personal, holds money figures. Local only,
# same as CLAUDE.md.
project_north_star.md

# Content & growth strategy -- kept local while the growth campaign is in progress
# (sequencing, account handle, backlog). Not committed to the public repo for now.
mma_content_strategy.md
content_bank.md
content_research/
technical_writeup.md
ChatGPT's suggestion for turning ude points dataset into content machine.docx
dataset refresh 2026_09_08 report.md

# New files (reports, plans, backing docs) default to local-only unless
# explicitly approved for the remote -- see CLAUDE.md's "Nothing new reaches
# the GitHub remote unless explicitly told to". Add each new one here as it's
# created, not after the fact.
similarity_and_power_improvement_plan.md
style_axes_story_test_report.md

# Claude Code local settings (attribution.commit/.pr disabled here) --
# machine-specific, never pushed.
.claude/settings.local.json
dynamic_accuracy_shrinkage_plan.md
